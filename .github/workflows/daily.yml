name: Daily Report
on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:
jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Daily report job"
